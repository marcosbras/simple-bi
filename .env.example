# Token do Cloudflare Tunnel (Zero Trust → Networks → Tunnels → seu túnel → "Install and run a connector").
# Copie este arquivo para ".env" e cole o token real ali (não versionado).
CLOUDFLARE_TUNNEL_TOKEN=
